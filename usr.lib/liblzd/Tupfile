include_rules

ifeq (@(LIB_LZD),y)
: foreach *.c |> !cc |> %B.o {obj}

: {obj} |> !ldso -lc -lm |> $(DIST)/usr/lib/liblzd.so

ifeq (@(BUILD_STATIC),y)
: {obj} |> !ar |> $(DIST)/usr/lib/liblzd.a
endif

endif
