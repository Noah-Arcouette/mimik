include_rules

ifeq (@(SHARE_LOCALE_EN_CA_UTF8_MAN_UTILITY),y)
FILES=

ifeq (@(BIN_UNAME),y)
FILES+=uname.md
endif

ifeq (@(BIN_TTY),y)
FILES+=tty.md
endif

ifeq (@(BIN_HEAD),y)
FILES+=head.md
endif

ifeq (@(BIN_LOGNAME),y)
FILES+=logname.md
endif

ifeq (@(BIN_ECHO),y)
FILES+=echo.md
endif

: foreach $(FILES) |> !txtcp \
	|> $(DIST)/usr/share/locale/en_CA.utf8/man/utility/%b

endif
