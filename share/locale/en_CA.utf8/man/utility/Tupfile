include_rules

ifeq (@(SHARE_LOCALE_EN_CA_UTF8_MAN_UTILITY),y)
FILES=

ifeq (@(USR_BIN_UNAME),y)
FILES+=uname.md
endif

ifeq (@(USR_BIN_TTY),y)
FILES+=tty.md
endif

ifeq (@(USR_BIN_HEAD),y)
FILES+=head.md
endif

ifeq (@(USR_BIN_LOGNAME),y)
FILES+=logname.md
endif

: foreach $(FILES) |> !txtcp \
	|> $(DIST)/usr/share/locale/en_CA.utf8/man/utility/%b

endif
