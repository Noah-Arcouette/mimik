# Name

This document may refer to multiple documents:

`magic` -- The concatenation of all `.mgc` source files


# Synopsis

`magic` the installed magic files tests for the `utility/file` command. See
`format/mgc`.


# Rationale

None.


# See Also

`format/mgc` -- Magic file format

`utility/file` -- The file utility
