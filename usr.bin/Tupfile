include_rules
FILES=

ifeq (@(USR_BIN_UNAME),y)
FILES+=uname.c
endif

ifeq (@(USR_BIN_TTY),y)
FILES+=tty.c
endif

ifeq (@(USR_BIN_HEAD),y)
FILES+=head.c
endif

ifeq (@(USR_BIN_LOGNAME),y)
FILES+=logname.c
endif

: foreach $(FILES) |> !ccld |> $(DIST)/usr/bin/%B
