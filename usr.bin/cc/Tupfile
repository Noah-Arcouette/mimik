include_rules

# toolchain build
: foreach *.c |> !host_cc   |> ./obj/%B.o {obj}
: {obj}       |> !host_ccld |> $(ROOT)/toolchain/cc | $(ROOT)/<toolchain>

: | $(ROOT)/<toolchain> |> $(ROOT)/toolchain/cc |>
